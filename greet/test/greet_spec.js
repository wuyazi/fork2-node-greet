var greet = require("../index.js");
var input_string = "haha --drunk";

describe('greet', function(){
  it("should greet aperson by name", function(){
    // console.dir(input_string);
    // console.log(greet(input_string.slice(1)));
    // expect(greet(input_string)[1]).to.be.null;
  });
});
